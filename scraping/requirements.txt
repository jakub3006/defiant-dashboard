requests>=2.31
beautifulsoup4>=4.12
pandas>=2.0
playwright>=1.40
yfinance>=0.2.40
# Stealth patches for Playwright (hides navigator.webdriver, fakes WebGL/
# canvas fingerprints, etc.). Optional — daily_scrape.py applies manual
# patches as fallback when the library is missing.
tf-playwright-stealth>=1.1.0
